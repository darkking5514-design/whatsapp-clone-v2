# Rename this file to .env when deploying so the frontend knows where the backend lives.
# For local development you can leave this unset; it defaults to http://localhost:5000/api

VITE_API_URL=http://localhost:5000/api
